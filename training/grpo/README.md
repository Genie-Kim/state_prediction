# NSR GRPO

GRPO here is an auxiliary one-shot format reward, not the primary retrieval objective.

Use SFT for the hidden-state retrieval loss. Use this folder only when you want to
reinforce that generated completions keep the expected `<emb>` token structure
without switching to multi-turn rollout.
